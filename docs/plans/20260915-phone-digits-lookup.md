# phoneDigits: поиск лида, сделки и контрагента по номеру телефона

Задача в трекере: [ECOSCRM-106](https://citeck.ecos24.ru/v2/dashboard?recordRef=emodel/ept-issue@ECOSCRM-106). Потребитель результата — COREDEV-510 (маршрут «запись разговора Mango Office → транскрибация → активность»).

## Overview

Чтобы по входящему номеру телефона находить лид или сделку, номер должен лежать в проиндексированной колонке в нормализованном виде. Сейчас телефон хранится в JSON-атрибуте `contacts[].contactPhone` и в скалярных полях контрагента, а предикатный поиск по содержимому JSON в ecos-data невозможен: путь `contacts.contactPhone` срезается в `alwaysFalse` и запрос молча возвращает ноль записей. Проверено на живых данных и для лида, и для сделки.

Решение: вычисляемый атрибут `phoneDigits` (TEXT, multiple, GIN-индекс) на типе `opportunity` (его наследуют `lead` и `deal`) и на типе `ecos-counterparty`. Значение — массив уникальных ключей по 10 цифр, полученных из всех телефонов записи одним и тем же алгоритмом нормализации.

Что это даёт:

- COREDEV-510 получает рабочий двухшаговый поиск: сначала контрагент по его `phoneDigits`, затем сделка по `OR(eq phoneDigits, in counterparty [refs])`, затем лид;
- пользователь впервые получает рабочий фильтр «Телефон» в журналах лидов и сделок — сейчас он молча не работает по той же причине.

**Симметрия — главное требование.** Один и тот же алгоритм применяется в трёх местах: скрипт на `opportunity`, скрипт на контрагенте и нормализация номера из события Mango в COREDEV-510. Расхождение хотя бы в одном шаге означает, что ключи не совпадут и поиск будет молча промахиваться. Спецификация алгоритма — в описании ECOSCRM-106, раздел «Нормализация номера».

## Context (from discovery)

**Файлы и компоненты:**

- `ecos-crm/src/main/resources/app/artifacts/model/type/opportunity.yml` — тип-родитель, 155 строк, атрибут `contacts` (JSON, multiple) на строке 146;
- `ecos-crm/src/main/resources/app/artifacts/model/type/deal.yml` — аспект `history-config` с `excludedAtts`;
- `ecos-crm/src/main/resources/app/artifacts/model/type/lead.yml` — аспекта `history-config` нет;
- `ecos-datalist/src/main/resources/app/artifacts/model/type/ecos-counterparty.yml` — `phone` (стр. 162), `cellPhone` (стр. 166), `contacts` (стр. 240), аспект `history-config` с `excludedAtts`;
- `ecos-crm/src/main/resources/app/artifacts/ui/journal/deals-journal.yml:107` и `leads-journal.yml:118` — колонка «Телефон», привязана к неработающему пути;
- `ecos-crm/src/main/resources/app/artifacts/app/patch/move-crm-deals-to-crm-workspace.yml` — готовый образец патча, создающего групповое действие;
- `ecos-crm/pom.xml` и `ecos-datalist/pom.xml` — сейчас без секции `<build>`.

**Особенности обоих проектов:**

- родитель `ecos-apps-simple-parent` 1.1.1 в обоих проектах (в `ecos-crm` обновлён с 1.0.3 коммитом COREDEV-463 от 14.09.2026), только артефакты: ни одного `.kt`/`.java`, нет `src/test`, нет собственного Spring-контекста;
- родитель задаёт `maven.compiler.source/target = 1.8` — пока компилировать нечего, это ни на что не влияет, но сломает сборку, как только появится `src/test/java` (локальный JDK 25 `source 8` не принимает);
- CI — `Jenkinsfile` из одной строки `ecosBuild()` с `ecos-build-config.yml: type: maven-ecos-apps`, пайплайн задан общей библиотекой и зовёт maven. Отдельный шаг в CI добавить нельзя, но всё, что цепляется к фазе `test`, попадает в сборку автоматически.

**Найденные соглашения платформы:**

- `ComputedAttType` — закрытый список: `SCRIPT`, `ATTRIBUTE`, `VALUE`, `TEMPLATE`, `COUNTER`, `NONE`. Точки расширения «посчитать Java-бином» нет;
- computed SCRIPT исполняется на **GraalJS** (`org.graalvm.js:js` 24.2.2), не на Nashorn. Привязки — `value`, `Records`, `log`;
- индекс создаётся только при явном `index: { enabled: true }`; для TEXT+multiple это GIN по `VARCHAR[]`, и `addColumnIndex` вызывается в том числе на пути `ALTER TABLE … ADD COLUMN`;
- пересчёт вычисляемых атрибутов на существующих записях — штатное групповое действие `update-calculated-atts` (выборка `admin-action-records-of-type` рекурсивно обходит дочерние типы).

**Стенд:** `tdcuosa` поднят, `ecos-crm` и `ecos-datalist` на нём развёрнуты (2 лида, 25 контрагентов).

## Принятые решения

Эти решения приняты при планировании и в ходе реализации не пересматриваются без явного согласования.

1. **Один план на оба репозитория.** Задачи явно указывают файлы и в `ecos-crm`, и в `ecos-datalist`. Причина: алгоритм нормализации пишется один раз и сразу согласованно в двух местах — расхождение между ними и есть главный риск задачи.
2. **Заполнение существующих записей — декларативными патчами**, а не ручным запуском действия. Патч отрабатывает автоматически на каждом стенде, и приёмку можно проверить автоматически.
3. **Скрипт остаётся на JS в YAML, а не на Java в микросервисе.** Выбора практически нет: модель типов другого способа не даёт. Реализация на Java означала бы слушатель `DbRecordChangedEvent`, а он (а) живёт внутри `ecos-model` — ядра платформы, где CRM-логике не место, отдельного микросервиса под CRM сейчас нет; (б) требует защиты от рекурсии, потому что запись меняется из слушателя её же изменения; (в) делает запись асинхронной — появляется окно, в которое звонок не найдёт сделку; (г) лишает нас штатного пересчёта, потому что «Обновить вычисляемые атрибуты» работает именно с computed-атрибутами, и заполнение существующих записей снова пришлось бы делать патчем на Kotlin.
4. **Модульные тесты — `mvn test` на настоящем GraalJS**, в maven-профиле с активацией по `<jdk>[17,)</jdk>`. Тест читает `config.fn` прямо из YAML, поэтому эталонную копию алгоритма вести не надо: единственный источник правды — сам YAML. Профиль нужен потому, что версия JDK на агенте Jenkins неизвестна, а сломать общую сборку нельзя.
5. **Колонка «Телефон» в журналах чинится в этом же плане**, отдельной задачей в конце.
6. **Порядок — TDD.** Таблица примеров в ECOSCRM-106 уже является готовой спецификацией, её можно перенести в тест до написания скрипта.

## Development Approach

- **Testing approach**: TDD — сначала тесты, затем скрипт.
- Каждая задача доводится до конца перед переходом к следующей.
- **Каждая задача обязана содержать новые или обновлённые тесты** для изменённого в ней кода — это не опция, а требование.
- **Все тесты должны проходить до начала следующей задачи**, исключений нет.
- **Если объём меняется по ходу — обновлять этот файл.**
- Команда прогона: `mvn test` в `ecos-crm` и в `ecos-datalist`. Полный лог писать в файл, в контекст тянуть только строки вердикта:
  ```bash
  mvn clean test > /tmp/mvn-phone-digits.log 2>&1
  grep -E "Tests run:.*(Failures|Errors)|BUILD (SUCCESS|FAILURE)|\[ERROR\]" /tmp/mvn-phone-digits.log | tail -40
  ```
  Ориентироваться на строки `Tests run: …` и `BUILD`, а не на код возврата.
- Обратная совместимость: существующие атрибуты и формы не трогаем, `phoneDigits` только добавляется.

## Testing Strategy

- **Модульные тесты** — обязательны в каждой задаче, где меняется скрипт или модель. Прогоняются на GraalJS, читая `config.fn` из YAML.
- **Приёмка на стенде** — обязательный и блокирующий этап, а не дополнение. Проверяется через Records API (Citeck MCP: `records_query`, `records_mutate`) и через Playwright для интерфейсной части.
- **Покрываем не только успешный путь.** Обязательно: альтернативные ветки (номер только в `contacts`; номер только в `phone`), негативные (короткий номер, строка без цифр, пустые контакты), краевые (12+ цифр, добавочный после запятой, дубликаты), повторяемость (повторный прогон патча ничего не ломает).
- Ветка поиска через контрагента на текущих данных почти не срабатывает (контрагент заполнен у 2% сделок и 6% лидов), поэтому проверяется **только на подготовленных записях**, созданных в рамках приёмки.

## Progress Tracking

- Выполненное отмечать `[x]` сразу, не накапливая.
- Новые обнаруженные задачи добавлять с префиксом ➕.
- Проблемы и препятствия помечать префиксом ⚠️.
- Если реализация отклоняется от плана — править план.

## What Goes Where

- **Implementation Steps** (с чекбоксами) — всё, что агент делает сам: правки файлов, тесты, развёртывание на локальный стенд `tdcuosa`, проверки через Records API и Playwright.
- **Post-Completion** (без чекбоксов) — то, что требует человека или внешних систем: ревью, выкладка на прод, согласование с COREDEV-510.

## Implementation Steps

### Task 1: Тестовая обвязка на GraalJS в ecos-crm

- [x] добавить в `ecos-crm/pom.xml` профиль с активацией `<jdk>[17,)</jdk>`: переопределить `maven.compiler.source` и `maven.compiler.target` на 17, подключить `org.junit.jupiter:junit-jupiter` и `org.graalvm.js:js` (обе в scope `test`), включить `maven-surefire-plugin`
- [x] создать `src/test/java/ru/citeck/ecos/crm/phonedigits/ComputedScriptRunner.java` — читает файл типа, достаёт `model.attributes[id=…].computed.config.fn`, исполняет его на GraalJS и возвращает результат как список строк; в скрипт подставляется заглушка `value` с методом `load(att)`
- [x] написать тест обвязки: тривиальный скрипт, возвращающий массив из двух строк, корректно доходит до раннера (проверяется сама связка, не бизнес-логика)
- [x] написать тест обвязки: если атрибут в YAML не найден, раннер падает с понятным сообщением, а не с NPE
- [x] прогнать `mvn test` — зелёный; убедиться, что сборка без профиля (на JDK ниже 17) по-прежнему проходит и тесты просто пропускаются

Итог Task 1: `ComputedScriptRunnerTest` — 12 тестов, `Tests run: 12, Failures: 0, Errors: 0`, `BUILD SUCCESS`.

➕ Дополнения к исходному списку задачи, понадобившиеся по ходу:

- в тестовые зависимости добавлен `org.yaml:snakeyaml` 2.2 — без парсера YAML раннеру нечем читать файл типа;
- в основных свойствах проекта задано `maven.test.skip=true`, а профиль `jdk17-tests` переключает его в `false`. Без этого сборка на JDK ниже 17 падала бы на компиляции тестовых исходников (junit и graal-js объявлены только в профиле). Проверено: `mvn clean package -P '!jdk17-tests'` даёт `Not compiling test sources`, `Tests are skipped.`, `BUILD SUCCESS` и собирает `target/classes/apps/ecos-crm.zip`;
- раннер разворачивает `PolyglotException` обратно в исходное исключение заглушки, иначе понятное сообщение о несогласованности теста и скрипта тонуло бы в обёртке;
- заглушка `value.load(att)` намеренно падает на атрибуте, которого нет в наборе данных теста: в тесте это всегда означает расхождение между скриптом и подготовленными данными, а не отсутствие значения.

### Task 2: Атрибут phoneDigits на opportunity

- [x] написать тесты по таблице примеров из ECOSCRM-106 — все 14 строк, включая `[your-phone]`, `630-20-10`, `US` и пустую строку (падают: атрибута ещё нет)
- [x] написать тесты на порядок шагов: `8-495-556-45-95, доб. 422` даёт `4955564595` и НЕ даёт `5564595422`; `+7 903 234-41-35, +7 495 123-45-67` даёт два ключа
- [x] написать тесты на краевые случаи: нет ни одного годного номера → пустой массив, а не `null`; два одинаковых номера дают один ключ; 12-значный иностранный номер обрезается до последних 10
- [x] добавить в `opportunity.yml` атрибут `phoneDigits`: `type: TEXT`, `multiple: true`, `computed: { type: SCRIPT, storingType: ON_MUTATE, config: { fn: … } }`, `index: { enabled: true }`; скрипт читает `contacts[].contactPhone` обычным циклом по индексу и собирает результат в литерал `[]` через `push`
- [x] прогнать `mvn test` — все тесты зелёные

Итог Task 2: `PhoneDigitsOpportunityTest` — 31 тест, вместе с обвязкой `Tests run: 43, Failures: 0, Errors: 0`, `BUILD SUCCESS`. Сборка артефактов без тестового профиля (`mvn clean package -P '!jdk17-tests'`) тоже зелёная и собирает `ecos-crm.zip` — YAML с новым атрибутом корректен для плагина сборки приложений.

Разделители в скрипте заданы одним регулярным выражением `/[,;#]|доб|вн\.|ext/i`. Регистронезависимость проверена и для кириллицы (`ДОБ`, `Вн.`), и для латиницы (`EXT`).

➕ Дополнения к исходному списку задачи, понадобившиеся по ходу:

- в скрипт добавлена защита от случая, когда `value.load` вернул одну строку вместо списка: `typeof phones === 'string'` → значение оборачивается в массив. Без этого цикл пошёл бы по символам строки. Проверка написана именно через `typeof`, а не через `Array.isArray`, потому что `Array.isArray` на загруженном java-списке зависит от опции `js.foreign-object-prototype`, а `typeof` — нет;
- тело нормализации вынесено в функцию `collectPhoneKeys(raw, target)` — именно она и есть тот фрагмент, который в Task 3 обязан совпасть с контрагентским посимвольно. Разное между типами остаётся только в прологе, который собирает исходные строки;
- покрыты дополнительные случаи сверх списка задачи: все шесть разделителей по отдельности, регистронезависимость, обе части вокруг разделителя достаточной длины дают по ключу, ровно 10 цифр и ровно 9 цифр, контакт без телефона (`null` в списке) не ломает остальные, порядок ключей повторяет порядок контактов, 11-значный номер не с 7/8 тоже теряет первую цифру (задокументированный побочный эффект).

### Task 3: Атрибут phoneDigits на ecos-counterparty

- [x] завести в `ecos-datalist/pom.xml` такой же профиль и перенести `ComputedScriptRunner` в `ecos-datalist/src/test/java/...` (обвязка дублируется намеренно: каждый проект проверяет свой YAML, общего места для кода у artifact-проектов нет)
- [x] написать тесты: номер только в `contacts[].contactPhone` при пустых `phone`/`cellPhone` попадает в `phoneDigits`; номер только в `phone`; номер только в `cellPhone`
- [x] написать тест: номера из всех трёх источников сливаются в один массив с дедупликацией, если совпадают
- [x] прогнать на контрагенте **ту же таблицу примеров**, что и в Task 2 — алгоритм нормализации обязан давать посимвольно те же ключи
- [x] добавить `phoneDigits` в `ecos-counterparty.yml` с теми же параметрами, скрипт читает три источника
- [x] прогнать `mvn test` в обоих проектах — зелёный

Итог Task 3: `ecos-datalist` — `PhoneDigitsCounterpartyTest` 62 теста плюс обвязка 12, `Tests run: 74, Failures: 0, Errors: 0`, `BUILD SUCCESS`; `ecos-crm` — прежние `Tests run: 43, Failures: 0, Errors: 0`, `BUILD SUCCESS`. Сборка артефактов `ecos-datalist` без тестового профиля (`mvn clean package -P '!jdk17-tests'`) даёт `Not compiling test sources`, `Tests are skipped.`, `BUILD SUCCESS` и собирает `ecos-common-data-list.zip`.

Таблица примеров ECOSCRM-106 прогоняется на контрагенте трижды — по разу на каждый источник (`phone`, `cellPhone`, `contacts[].contactPhone`), поэтому расхождение в любом из них будет видно сразу, а не только в том источнике, который попал в тест.

Функция `collectPhoneKeys` перенесена в `ecos-counterparty.yml` посимвольно из `opportunity.yml` (сверено программно: совпадают все 1100 символов). Различается только пролог: контрагент читает три источника вместо одного и складывает их в общий массив `result`, за счёт чего дедупликация работает не только внутри источника, но и между ними. Порядок ключей — `phone`, `cellPhone`, затем контакты в порядке их следования; он зафиксирован тестом, чтобы значение не «прыгало» между пересчётами.

➕ Дополнения к исходному списку задачи, понадобившиеся по ходу:

- вместе с раннером в `ecos-datalist` перенесены `ComputedScriptRunnerTest` и его набор данных `src/test/resources/computed-script-runner/harness-type.yml`. Без набора данных перенесённая обвязка падает, а без самих тестов обвязки во втором проекте некому проверить допущения о поведении GraalJS — они там ровно те же;
- три источника читаются тремя отдельными вызовами `value.load('<строка>')`, а не одним вызовом со списком атрибутов. Причина — ограничение, найденное в Task 1: у загруженного JSON-объекта `Object.keys` возвращает пустой список, и хотя чтение по известному ключу работает, вариант со строковым аргументом уже проверен на `opportunity` и не зависит от способа обёртки map-а;
- в тестах покрыты сверх списка задачи: слияние трёх разных номеров в три ключа, дедупликация внутри самого списка контактов, порядок ключей при нескольких контактах, отсутствие телефонов во всех трёх источниках (пустой массив, а не `null`), контакт без телефона (`null` в списке), а также разделители, регистронезависимость и краевые длины — те же, что и на `opportunity`.

### Task 4: Исключить phoneDigits из истории изменений

- [x] добавить `phoneDigits` в `excludedAtts` аспекта `history-config` в `deal.yml`
- [x] добавить `phoneDigits` в `excludedAtts` аспекта `history-config` в `ecos-counterparty.yml`
- [x] разобраться с `lead.yml`: аспекта `history-config` там сейчас нет — выяснить, пишется ли история лида по умолчанию, и либо добавить аспект с исключением, либо записать в план обоснование, почему не нужно
- [x] прогнать `mvn test` в обоих проектах — зелёный

Итог Task 4: `ecos-crm` — новый `PhoneDigitsHistoryExclusionTest` на 6 тестов, вместе с прежними `Tests run: 49, Failures: 0, Errors: 0`, `BUILD SUCCESS`; `ecos-datalist` — новый `PhoneDigitsHistoryExclusionTest` на 4 теста, вместе с прежними `Tests run: 78, Failures: 0, Errors: 0`, `BUILD SUCCESS`. Сборка артефактов без тестового профиля (`mvn clean package -P '!jdk17-tests'`) зелёная в обоих проектах — YAML с новым аспектом корректен для плагина сборки приложений.

**Решение по `lead.yml`: аспект добавлен.** История лида пишется по умолчанию — это проверено на стенде `tdcuosa`, а не выведено из документации. В базе `citeck_history` (таблица `history_records`, связь с записью через `document_ref_id` → `ed_record_ref.__ext_id`) у лидов уже лежит 19 событий `node.updated` и 5 `node.created`, хотя аспекта `history-config` у типа `lead` нет и от родительского `opportunity` наследовать нечего — у него аспектов не объявлено вовсе. Каждое событие `node.updated` — это отдельная запись на один изменённый атрибут с готовой подписью (например `{"ru":"Активности: удалено"}`), то есть без исключения каждое сохранение лида добавляло бы в виджет «История событий» строку об изменении `phoneDigits`. Поэтому в `lead.yml` заведён собственный аспект `history-config` с `disableHistory: false`, `excludedAtts: [phoneDigits]` и `inheritConfig: false` — те же параметры, что у `deal.yml`, кроме исключения `crm-bitrix24:bitrixSyncDate`: у лида нет атрибутов синхронизации с Битрикс24.

➕ Дополнения к исходному списку задачи, понадобившиеся по ходу:

- в обоих проектах заведён `PhoneDigitsHistoryExclusionTest`, читающий аспект прямо из YAML: проверяется наличие `phoneDigits` в `excludedAtts`, сохранение прежнего исключения `crm-bitrix24:bitrixSyncDate` (новое значение добавлено к списку, а не заменило его), `disableHistory: false` и совпадение исключаемого идентификатора с идентификатором вычисляемого атрибута. Последняя проверка нужна потому, что опечатка в `excludedAtts` ничем себя не проявляет: история просто продолжит записываться;
- отдельный тест фиксирует, что `opportunity` не объявляет аспект `history-config`, а `lead` объявляет его сам. Если аспект однажды перенесут на родительский тип, тест покажет, что исключение у лида стало дублирующим;
- вспомогательный разбор YAML в тестах считает отсутствие секции `aspects` отсутствием аспектов, а не ошибкой — иначе проверка на `opportunity`, где секции нет, падала бы вместо того, чтобы дать ответ.

### Task 5: Патчи заполнения существующих записей

- [x] создать `ecos-crm/src/main/resources/app/artifacts/app/patch/fill-opportunity-phone-digits.yml`: `type: mutate`, `targetApp: emodel`, запись `emodel/group-action@` с `values: { type: admin-action-records-of-type, config: { typeRef: emodel/type@opportunity } }` и `execution: { type: update-calculated-atts }`
- [x] создать `ecos-datalist/src/main/resources/app/artifacts/app/patch/fill-counterparty-phone-digits.yml` — то же самое для `emodel/type@ecos-counterparty`
- [x] сверить структуру обоих патчей с образцом `move-crm-deals-to-crm-workspace.yml` (там исполнение `update-workspace`), не скопировав `manual: true` по инерции
- [x] принять и записать в этот файл решение по `manual`: образец помечен `manual: true`, потому что это разовая массовая миграция; здесь затрагивается 5712 записей, и автоматический запуск в момент выкладки на прод может быть нежелателен
- [x] прогнать `mvn test` в обоих проектах — зелёный

Итог Task 5: `ecos-crm` — новый `PhoneDigitsFillPatchTest` на 5 тестов, вместе с прежними `Tests run: 54, Failures: 0, Errors: 0`, `BUILD SUCCESS`; `ecos-datalist` — новый `PhoneDigitsFillPatchTest` на 4 теста, вместе с прежними `Tests run: 82, Failures: 0, Errors: 0`, `BUILD SUCCESS`. Сборка артефактов без тестового профиля (`mvn clean package -P '!jdk17-tests'`) зелёная в обоих проектах, и оба файла патчей действительно попадают в zip приложения (`artifacts/app/patch/fill-opportunity-phone-digits.yml` и `artifacts/app/patch/fill-counterparty-phone-digits.yml`).

**Решение по `manual`: флаг не ставится, патчи применяются автоматически.** Это прямое следствие пункта 2 раздела «Принятые решения»: заполнение существующих записей делается декларативным патчем именно для того, чтобы он отрабатывал на каждом стенде сам и приёмку можно было проверить автоматически. Что означает флаг — проверено по коду `EcosPatchService` (ecos-apps): выборка задания патчей берёт в работу статус `PENDING` только у записей с `manual: false`, поэтому патч с `manual: true` остаётся в `PENDING` до тех пор, пока оператор не нажмёт «Применить» в журнале «Патчи». Для образца `move-crm-deals-to-crm-workspace.yml` это оправдано — разовый перенос записей между рабочими пространствами, — а здесь ручной запуск означал бы стенд с незаполненным `phoneDigits` и молча неработающим поиском по телефону, то есть ровно ту ситуацию, которую задача устраняет.

Опасение про массовость (5712 записей на проде) снимается не флагом `manual`, а тем, как патч выполняется: групповое действие обрабатывает записи своими порциями на стороне emodel, а патч создаёт ровно одну запись `emodel/group-action@`. Секция `batch` патча здесь не применима — она нарезает массив внутри `config` патча, а массив `config.records` состоит из одного элемента. Оценка времени работы на проде вынесена в раздел Post-Completion, где она и была: это ручная проверка на данных прода после выкладки.

➕ Дополнения к исходному списку задачи, понадобившиеся по ходу:

- в обоих проектах заведён `PhoneDigitsFillPatchTest`, читающий YAML патча: тип исполнения, `typeRef`, `targetApp`, наличие `date`, ровно одна создаваемая запись с идентификатором `emodel/group-action@` и отсутствие `manual: true`. Отдельным тестом проверяется, что `typeRef` указывает на тип, который действительно объявляет `phoneDigits` — при ссылке на другой тип патч отработал бы успешно и не изменил ничего;
- тест в `ecos-crm` дополнительно читает сам образец `move-crm-deals-to-crm-workspace.yml` и сверяет с ним набор атрибутов группового действия, а также фиксирует, что образец помечен `manual: true` — именно от копирования этого флага тест и защищает. В `ecos-datalist` образца нет (он лежит в другом репозитории, и тест не может читать чужое дерево), поэтому там ожидаемая структура выписана в тесте явно;
- `config` у `execution` не задаётся: `UpdateCalculatedAttsAdminAction` в ecos-model объявлен с `Config = Unit`, и штатное действие `admin-update-calculated-atts.yml` тоже передаёт только `type`;
- `workspaces` в конфигурации выборки не задаётся, хотя `opportunity`, `lead` и `deal` имеют `workspaceScope: PRIVATE`. Проверено по коду: `AdminActionRecordsOfTypeValues` выполняет запрос под системным пользователем, а `WorkspaceServiceImpl.getAvailableWorkspacesToQuery` при пустом списке запрошенных рабочих пространств возвращает для системного пользователя пустое множество, из-за чего `DbRecordsQueryDao` не добавляет условие по рабочему пространству вовсе. То есть патч обойдёт записи во всех рабочих пространствах, включая перенесённые в `crm-workspace`;
- ⚠️ **порядок относительно выкладки типа платформой не гарантирован.** Для типа патча `mutate` метаданные `dependsOnRefs` пусты (проверено `PatchTypeMetaRegistryTest` в ecos-apps: `getMeta("eapps", "mutate").dependsOnRefs` пуст, в отличие от `import-data`), поэтому патч не ждёт развёртывания артефакта типа. Если он отработает раньше, чем на стенд приедет `opportunity.yml` с `phoneDigits`, он завершится успешно и не заполнит ничего. Практическая защита: это видно на приёмке Task 6 (`records_query` по существующим записям), а перезапуск делается увеличением `date` патча либо действием «Применить» в журнале «Патчи». Повторный прогон проверяется отдельным пунктом Task 7.

### Task 6: Развернуть на стенд и проверить базовое поведение

- [x] собрать оба проекта (`mvn clean package -DskipTests`) и скопировать `target/classes/apps/*.zip` в каталог ecos-apps стенда `tdcuosa`
- [x] убедиться по логам `citeck_eapps_tdcuosa_default`, что оба приложения в статусе DEPLOYED без ошибок (обязательно с `--tail` и `grep`, без выгрузки лога целиком)
- [x] проверить SQL-запросом к postgres стенда, что колонка под `phoneDigits` создана в таблицах обоих типов как `VARCHAR[]` и что по ней есть GIN-индекс (точное имя колонки выяснить по `information_schema`, а не угадывать)
- [x] проверить, что оба патча отработали — статус в журнале «Патчи»
- [x] проверить через `records_query`, что у существующих записей стенда `phoneDigits` заполнен, а `eq phoneDigits == <ключ>` находит запись

Итог Task 6: оба приложения выложены на стенд `tdcuosa` и работают, колонки и индексы созданы, патчи применились, поиск по ключу находит запись.

**Сборка и выкладка.** `mvn clean package -DskipTests` — `BUILD SUCCESS` в обоих проектах; `ecos-crm.zip` (252 КБ) и `ecos-common-data-list.zip` (69 КБ) скопированы в `~/Library/Application Support/Citeck/launcher/ws/DEFAULT/ns/tdcuosa/rtfiles/app/eapps/ecos-apps/`. Перед копированием содержимое архивов проверено: `phoneDigits` есть в `opportunity.yml`, `deal.yml`, `lead.yml` и `ecos-counterparty.yml`, оба файла патчей лежат в `artifacts/app/patch/`.

**Статусы развёртывания** (лог `citeck_eapps_tdcuosa_default`, только отфильтрованные строки): `model/type` — `Success: 4 Failed: 0` (`currency`, `ecos-counterparty`, `opportunity`, `deal`), `app/patch` — `Success: 3 Failed: 0` (в том числе оба наших патча), `ui/menu` и `model/workspace-template` — без ошибок. Строк уровня ERROR за время выкладки — ноль. Тип `lead` прошёл цепочку `DEPLOYED → DEPS_WAITING → DRAFT → DEPLOYED` (ждал родителя `opportunity`) и через 10 секунд после остальных перешёл в `DEPLOYED` — это штатное поведение, а не сбой, но при проверке выкладки на него нужно закладывать отдельный проход наблюдателя артефактов.

**Колонки и индексы** (`citeck_emodel`, схема `ecos_data`, имена взяты из `information_schema.columns` и `pg_indexes`):

| Таблица | Колонка | Тип | Индекс |
|---|---|---|---|
| `t_lead` | `phoneDigits` | `ARRAY` / `_varchar` (VARCHAR[]) | `t_lead_phoneDigits_idx` — `USING gin ("phoneDigits")` |
| `t_deal` | `phoneDigits` | `ARRAY` / `_varchar` | `t_deal_phoneDigits_idx` — `USING gin ("phoneDigits")` |
| `t_ecos_counterparty` | `phoneDigits` | `ARRAY` / `_varchar` | `t_ecos_counterparty_phoneDigits_idx` — `USING gin ("phoneDigits")` |

Уточнение к формулировке задачи «в таблицах обоих типов»: собственной таблицы у типа `opportunity` нет — он родительский, а записи лежат в таблицах наследников `t_lead` и `t_deal`. Поэтому колонок и индексов не два, а три, и проверять надо все три.

**Патчи.** В `citeck_eapps.public.ecos_patch` обе записи в статусе `APPLIED`, `manual = false`, `errorsCount = 0`, `lastError` пуст; `patchResult.completed = true`, каждый патч создал ровно одну запись группового действия (`emodel:c6ud2azf7fno/group-action@…`). То же самое видно и через журнал «Патчи» (`records_query` по `eapps/ecos-patch`, предикат `contains patchId "phone-digits"` — две записи, обе `APPLIED`). Опасение из Task 5 о том, что патч может отработать раньше выкладки типа, на этом стенде не подтвердилось: типы перешли в `DEPLOYED` в 10:55:06, патчи развернулись в 10:55:08.

**Заполнение существующих записей.** `phoneDigits` не `null` ни у одной записи: `t_lead` — 5 из 5, `t_deal` — 131 из 131, `t_ecos_counterparty` — 25 из 25. Непустые ключи там, где телефон действительно есть: лид `6dad3116` → `{9162042100}` из `+79162042100`, контрагенты `66ae3568` → `{4950000001}` из `+7 495 000-00-01` и `cc638771` → `{4951112233}` из `+7 495 111-22-33`. Оба контрагента — как раз тот случай, когда телефон заполнен только в `contacts`, а `phone` и `cellPhone` пусты.

**Поиск через Records API.** `emodel/lead` с предикатом `eq phoneDigits == "9162042100"` возвращает ровно одну запись («Лид №2: Тест лид»); `emodel/ecos-counterparty` с `eq phoneDigits == "4951112233"` — ровно одну запись (ООО «regr-20260816-r1-R-r7-client»). Запрос к `emodel/deal` по существующему ключу лида отрабатывает без ошибки и возвращает 0 записей — предикат на таблице сделок тоже рабочий, просто ни у одной сделки стенда телефона в `contacts` нет.

Попутно подтвердилась негативная ветка на живых данных: у контрагента `380b8852` в `phone` лежит строка `+`, и `phoneDigits` у него — пустой массив, а не `null` и не ключ из нуля цифр.

➕ Наблюдения, найденные при проверке (обе — не дефекты текущей реализации, но их надо учесть):

- у лида `964c053f` массив `contacts` имеет другую структуру: ключи `phone`, `email`, `fullName` вместо `contactPhone`, `contactEmail`, `contactFio` (запись создана регрессионным прогоном). Скрипт читает `contactPhone` по спецификации ECOSCRM-106 и такой номер не видит. Менять скрипт под вторую структуру не нужно: в модели контакт описан полями `contact*`, а разнобой в данных — вопрос к тому, кто их создавал. Но при проверке полноты заполнения на проде это даст расхождение «телефон в записи вижу, ключа нет», и объяснение должно быть под рукой;
- у типа `deal` есть собственный скалярный атрибут `phone`, помеченный в `deal.yml` комментарием `#Deprecated`; на стенде он непустой у 9 сделок из 131. В `phoneDigits` он не попадает — скрипт на `opportunity` читает только `contacts[].contactPhone`. Для COREDEV-510 это означает, что звонок с такого номера сделку не найдёт. Решение принимать в Task 8 или отдельной задачей: поле объявлено устаревшим, и расширение скрипта под него противоречило бы требованию симметрии с нормализацией на стороне Mango, если такое же поле не появится у контрагента и лида.

### Task 7: Приёмка на стенде — основные, альтернативные и негативные ветки

- [x] лид без контрагента находится по номеру из своих контактов
- [x] подготовить сделку с контрагентом и проверить двухшаговый поиск: контрагент по `phoneDigits` → сделка по `in counterparty [refs]`
- [x] подготовить лид с контрагентом и проверить тот же двухшаговый поиск
- [x] подготовить контрагента, у которого телефон заполнен **только** в `contacts`, а `phone` и `cellPhone` пусты — поиск обязан сработать
- [x] проверить пересчёт: добавление, изменение и удаление контакта обновляют `phoneDigits` записи; изменение `phone` у контрагента обновляет его собственный `phoneDigits` без пересохранения сделки
- [x] негативные и краевые ветки на стенде: номер короче 10 цифр и строка без цифр дают пустой массив, а не `null`; поиск по несуществующему ключу возвращает 0 записей без ошибки; повторный прогон патча (увеличением `date`) не ломает уже заполненные значения
- [x] записать в этот файл, какие случаи проверены и какие проверить не удалось

Итог Task 7: приёмка на стенде `tdcuosa` пройдена полностью — проверены все семь пунктов, ни один случай не остался непроверенным. Интерфейсная часть (фильтр «Телефон» в журналах) в Task 7 намеренно не проверялась: колонка журнала чинится в Task 8, там же и запланирован прогон через Playwright. Модульные наборы после приёмки прогнаны заново: `ecos-crm` — `Tests run: 54, Failures: 0, Errors: 0`, `BUILD SUCCESS`; `ecos-datalist` — `Tests run: 82, Failures: 0, Errors: 0`, `BUILD SUCCESS`.

**Записи, созданные для приёмки** (оставлены на стенде намеренно — на них же проверяется фильтр журнала в Task 8):

| Запись | Идентификатор | Роль в проверке |
|---|---|---|
| Контрагент «ООО «ECOSCRM-106 контакты-только»» | `emodel/ecos-counterparty@942213eb-cae0-4632-bd04-bd45138b4e37` | телефон только в `contacts`, затем добавлен `phone` |
| Сделка №204 | `emodel/deal@0e40e452-c54e-4411-b7fe-284f94461a81` | сделка с контрагентом, затем собственные контакты |
| Лид №7 | `emodel/lead@467d5800-3861-4a3c-b8b4-b82dc466a314` | лид с контрагентом, затем негодные номера |

**1. Лид без контрагента по своему номеру.** Лид `6dad3116` («Лид №2: Тест лид»), `counterparty` пуст, контакт `+79162042100`. Запрос `emodel/lead` с предикатом `eq phoneDigits == "9162042100"` вернул ровно одну запись — этот лид.

**2. Двухшаговый поиск сделки через контрагента.** Шаг 1: `emodel/ecos-counterparty`, `eq phoneDigits == "4957771122"` → одна ссылка на подготовленного контрагента. Шаг 2: `emodel/deal`, `in counterparty [<ссылка>]` → Сделка №204. Собственный `phoneDigits` сделки при этом пуст, то есть найдена она именно через контрагента, а не по своим контактам. Дополнительно проверена объединённая форма запроса, которую будет строить COREDEV-510 — `or(eq phoneDigits "4957771122", in counterparty [<ссылка>])`: одна запись, ошибки нет.

**3. Двухшаговый поиск лида через контрагента.** Тот же второй шаг на `emodel/lead` вернул Лид №7 — ветка работает одинаково для обоих наследников `opportunity`.

**4. Контрагент с телефоном только в `contacts`.** У подготовленного контрагента `phone` и `cellPhone` пусты, номер лежит только в `contacts[0].contactPhone` = `+7 495 777-11-22`. `phoneDigits` = `["4957771122"]`, поиск по ключу находит запись. Та же ветка подтверждена и на существующих данных стенда — контрагенты `66ae3568` и `cc638771` (см. Task 6).

**5. Пересчёт при изменении данных.**

| Действие | Результат |
|---|---|
| в сделку добавлен контакт `8-495-556-45-95, доб. 422` | `["4955564595"]` — добавочный номер отрезан, ключа `5564595422` нет |
| контакты заменены на `+7 903 234-41-35` и `+7 495 123-45-67` | `["9032344135","4951234567"]`; поиск по старому ключу `4955564595` даёт 0 записей, по новому `4951234567` — сделку |
| из двух контактов удалён один | остался `["4951234567"]` |
| удалены все контакты | пустой массив (в базе `{}`, `phoneDigits is null` = `false`) |
| у контрагента заполнен `phone` = `+7 (812) 555-33-44` | его `phoneDigits` стал `["8125553344","4957771122"]` — оба источника слились |

Отдельно проверено, что изменение контрагента не трогает сделку: `__modified` сделки до и после правки телефона контрагента одинаков (`2026-09-15 10:58:40.771244+00`), собственный `phoneDigits` сделки остался пустым, а двухшаговый поиск по новому ключу контрагента сделку всё равно находит. Это ровно то поведение, ради которого скрипт на `opportunity` не читает `counterparty.phoneDigits`: значение не может устареть, потому что его там просто нет.

**6. Негативные и краевые ветки.**

- в контакты Лида №7 положены заведомо негодные значения — `630-20-10` (8 цифр) и строка `нет телефона`; в базе `phoneDigits` = `{}`, проверка `is null` даёт `false`. То есть краевой случай даёт пустой массив, а не `null`, как и требует спецификация;
- `eq phoneDigits == "0000000000"` на `emodel/lead`, `emodel/deal` и `emodel/ecos-counterparty` — 0 записей, ответ без ошибки во всех трёх источниках;
- повторный прогон патчей проверен тремя способами и подробно расписан ниже.

**Повторный прогон патчей.** Проверка сделана в три прогона, чтобы отделить «ничего не сломалось» от «пересчёт действительно выполнялся»:

1. дата в обоих файлах патчей поднята до `2026-09-15T10:00:00Z`, оба проекта пересобраны и выложены на стенд. Обе записи патчей снова `APPLIED`, `errorsCount = 0`, `lastError` пуст. Контрольные суммы всех значений `phoneDigits` (md5 по `__ext_id` + значение, отсортировано) до и после прогона совпали для всех трёх таблиц: `t_lead` `9c74f80c…`, `t_deal` `67feae11…`, `t_ecos_counterparty` `0df7bb89…`. Ни одного `null` не появилось;
2. совпадение контрольных сумм само по себе ещё не доказывает, что пересчёт выполнялся, — тот же результат дал бы патч, не сделавший ничего. Поэтому перед третьим прогоном значение лида `6dad3116` намеренно испорчено прямо в базе на `{0000000001}`, а дата патча поднята до `10:30:00Z`. После прогона значение восстановилось в `{9162042100}` — групповое действие действительно перебирает записи и пересчитывает атрибут;
3. четвёртый прогон (дата `10:45:00Z`, снова через пересборку и выкладку) — снимки до и после опять совпали (`t_lead` `9c74f80c…`, `t_deal` `c34aac00…`; у сделок сумма отличается от первого прогона потому, что между прогонами Сделке №204 добавлены контакты, а не из-за патча).

После проверок дата в обоих файлах патчей возвращена к исходной `2026-09-15T00:00:00Z`, проекты пересобраны и выложены на стенд: рабочие деревья чистые, записи патчей на стенде снова `APPLIED` с исходной датой. Побочный эффект приёмки в репозитории не остался.

Попутно уточнён механизм перезапуска, который стоит знать при выкладке на прод: статус патча сбрасывается в `PENDING` только если новая дата **больше** прежней (`EcosPatchConfig`, сравнение `dateAfter.isAfter(dateBefore)`), поэтому выкладка артефакта с более ранней датой ничего не перезапускает. Ещё одна особенность: правка даты прямо в записи патча через Records API сбрасывает статус в `PENDING`, но задание исполнения запускается не мгновенно — оно просыпается по событию выкладки артефактов, поэтому «дёрнуть» патч без выкладки получается с задержкой.

➕ Наблюдения, найденные при приёмке (к `phoneDigits` отношения не имеют, но мешали работе и могут помешать в COREDEV-510):

- ⚠️ **сделка или лид, созданные через Records API без явного `manager`, недоступны даже администратору.** `records_mutate` отвечает `ok: true` и возвращает идентификатор, запись реально появляется в `t_deal`/`t_lead` со всеми переданными атрибутами, но последующее чтение возвращает пустые значения, `records_query` её не находит вовсе, а попытка изменить падает с HTTP 500 и `Access denied to record …` в логе emodel. Причина — вычисляемая роль `manager` в `opportunity.yml`: при пустом `manager` она отдаёт `GROUP_crm-manager`, а администратор в эту группу не входит, и других ролей у записи нет. Выглядит это как «мутация молча не сработала» и стоило нескольких попыток при подготовке записей приёмки. Записи приёмки создавались уже с `manager` = `emodel/person@admin`. Осадок остаётся: пользователь без роли создаёт запись, которую сам же не видит;
- предикат `not-eq counterparty ""` на сделках возвращает в том числе записи с пустым `counterparty`, то есть для отбора «контрагент заполнен» он не годится — нужен `not-empty`. Для COREDEV-510 это важно: предикаты по ссылочному атрибуту надо строить через `in`/`not-empty`, а не через сравнение с пустой строкой.

### Task 8: Колонка «Телефон» в журналах лидов и сделок

- [x] перевести колонку «Телефон» на `phoneDigits` в `deals-journal.yml` и `leads-journal.yml` так, чтобы и отображение, и фильтрация работали
- [x] принять и записать решение по `ecos-crm-citeck/.../deals-community-journal.yml:72` — та же поломка, но другой репозиторий: чинить здесь или отдельной задачей
- [x] пересобрать и выложить на стенд, проверить через Playwright: открыть журнал сделок и журнал лидов, отфильтровать по телефону существующей записи, убедиться, что запись находится
- [x] проверить `browser_console_messages` и `browser_network_requests` на отсутствие JS-ошибок и ответов 4xx/5xx
- [x] осмотреть страницу целиком на любые дефекты, не только на проверяемый: вёрстка, локализация, пустые значения, `null`/`undefined` в ячейках — найденное записать в этот файл
- [x] прогнать `mvn test` — зелёный

Итог Task 8: фильтр «Телефон» в журналах лидов и сделок работает, отображение осталось прежним — человекочитаемым номером. Проверено на стенде `tdcuosa` через Playwright, набор тестов `ecos-crm` — `Tests run: 60, Failures: 0, Errors: 0`, `BUILD SUCCESS`.

**Как именно чинилось — и почему не так, как было записано в задаче.** Формулировка «перевести колонку на `phoneDigits`» предполагала замену атрибута колонки. Делать этого нельзя: `phoneDigits` — это внутренние ключи из десяти цифр, и в ячейке пользователь увидел бы `4951234567` вместо `+7 495 123-45-67`. Проверка на стенде показала, что загрузка пути внутрь JSON работает (`records_query` по `contacts.contactPhone` у лида `6dad3116` вернул `+79162042100`) — не работает только предикат. Поэтому атрибут отображения оставлен прежним, а фильтрация перенаправлена на вычисляемый атрибут:

```yaml
    attribute: contacts.contactPhone
    searchConfig:
      searchAttribute: phoneDigits
```

`searchAttribute` — штатное поле `ColumnSearchConfig` в `ecos-uiserv`; `ecos-ui` подменяет по нему атрибут предиката перед отправкой запроса (`JournalsConverter.searchConfigProcessed` в `ecos-ui/src/dto/journals.js`). Развести отображение и фильтрацию через `attSchema` было нельзя: `attSchema` — это только внутренняя схема, она приписывается к тому же атрибуту (`journalColumnsResolver.js`), а не заменяет его.

**Ограничение, которое надо знать пользователю: в фильтр вводится ключ из десяти цифр.** Предикат по multiple-колонке в `ecos-data` превращается не в поиск подстроки, а в пересечение массивов — `"phoneDigits" && ARRAY[?]` (`DbEntityRepoPg.toSqlCondition`, ветка `columnDef.multiple`). Это верно и для `contains`, и для `eq`, и для `in`. Поэтому `9162042100` находит запись, а `+7 916 204-21-00` — нет. Обойти это в рамках принятого формата ключа нельзя: нормализовать введённый текст на стороне клиента журнал не умеет, а `searchConfig.delimiters` только разрезает строку на несколько предикатов, но не убирает из неё лишние символы. Перед сдачей задачи это надо проговорить с заказчиком: либо оставляем как есть и пишем в подсказке, что вводить надо цифры, либо отдельной задачей добавляем на стороне `ecos-ui` нормализацию значения фильтра для колонок с `searchAttribute`.

**Решение по `ecos-crm-citeck/.../deals-community-journal.yml`: чиним здесь же, тем же изменением, отдельным коммитом в своём репозитории.** Обоснование: журнал «Сделки Комьюнити» построен по тому же типу `emodel/type@deal`, то есть `phoneDigits` у него уже есть, а правка — одна декларативная строка, дословно повторяющая проверенную. Разносить одинаковую однострочную правку по двум задачам дороже, чем сделать её сразу. Что при этом надо помнить: на стенде `tdcuosa` приложение `ecos-crm-citeck` не развёрнуто (в каталоге ecos-apps лежат только `ecos-crm.zip` и `ecos-common-data-list.zip`), поэтому через Playwright эта правка не проверялась — она опирается на проверку идентичной колонки в `deals-journal.yml`. И выкладывать её можно только после выпуска `ecos-crm` с `phoneDigits`: без родительского типа атрибут в предикате не существует.

**Проверка на стенде через Playwright.** Приложение пересобрано и выложено, `uiserv` отдаёт обновлённую конфигурацию (`records_query` по `uiserv/journal@deals-journal` и `uiserv/journal@leads-journal` — у колонки `phone` `searchConfig.searchAttribute = phoneDigits`). Журналы открывались в рабочем пространстве CRM (`ws=crm-workspace`; в личном пространстве администратора записей нет и журнал пуст — на это стоит закладываться при любой следующей проверке журналов).

| Ветка | Действие | Результат |
|---|---|---|
| основная, лиды | фильтр «Телефон» = `9162042100` | 1 запись из 3: Лид №2, в ячейке `+79162042100` |
| основная, сделки | фильтр «Телефон» = `4951234567` | 1 запись из 43: Сделка №204, в ячейке `+7 495 123-45-67` |
| альтернативная (форматированный ввод) | фильтр «Телефон» = `+7 916 204-21-00` | «Нет данных» — ожидаемо, см. ограничение выше |
| негативная | фильтр «Телефон» = `9999999999` на сделках | «Нет данных», ошибки нет |
| сброс фильтра | значение очищено в обоих журналах | вернулись все записи: лиды 3, сделки 43 |

Отправленный предикат снят из тела запроса `q_emodel_deal`: `{"t":"contains","att":"phoneDigits","val":"4951234567"}` — подмена атрибута действительно происходит на клиенте, на сервер уходит уже `phoneDigits`.

**Консоль и сетевые запросы.** Все 13 запросов `records/query`, обслуживающих журналы, вернули 200. Ошибок, связанных с `phoneDigits` или журналами, нет. Найдены три посторонние ошибки, ни одна не вызвана этой задачей:

- `500` на `records/query?k=q_integrations_import_data_variant` и соответствующая ошибка в консоли `App is not available: 'integrations'` — на стенде не поднято приложение `integrations`, интерфейс всё равно его опрашивает при открытии журнала;
- `401` на `gateway/uiserv/api/theme/active/image/favicon` — запрос значка темы уходит без авторизации;
- `net::ERR_FAILED` на `chrome-extension://invalid/` — артефакт браузера, запускаемого Playwright, к приложению отношения не имеет.

➕ Замечено при осмотре страниц целиком (к `phoneDigits` отношения не имеет):

- после очистки значения фильтра воронка в заголовке колонки «Телефон» остаётся подсвеченной синим, хотя фильтр уже не применяется (записи вернулись полностью). Мелкий дефект интерфейса журнала, воспроизводится и в лидах, и в сделках;
- в журнале сделок у двух записей в колонке «ФИО» стоят значения `.` и `..` — мусор в данных стенда, не дефект интерфейса;
- локализация в обоих журналах ровная, пустые ячейки выглядят пустыми, `null`/`undefined` в них нет, вёрстка не съезжает.

### Task 9: Проверка приёмочных критериев

- [ ] сверить реализованное со списком «Приёмочные критерии» в ECOSCRM-106 — каждый пункт отдельно
- [ ] убедиться, что все краевые случаи из раздела Testing Strategy закрыты
- [ ] прогнать полный набор тестов в обоих проектах
- [ ] убедиться, что в плане запроса виден GIN-индекс, а не полное сканирование (`EXPLAIN` на стенде)
- [ ] проверить, что ни один пункт не остался помечен ⚠️ без объяснения

### Task 10: Документация

- [ ] обновить описание ECOSCRM-106, если по ходу выяснились расхождения со спецификацией
- [ ] описать в `ecos-crm/docs/` формат ключа `phoneDigits` и правила нормализации — это контракт для COREDEV-510
- [ ] отметить в задаче COREDEV-510, что поле готово и каким алгоритмом считается ключ

## Technical Details

**Формат значения.** Массив уникальных строк по 10 цифр. При отсутствии годных номеров — пустой массив, никогда не `null`.

**Алгоритм нормализации** (полная спецификация с таблицей примеров — в ECOSCRM-106, порядок шагов менять нельзя):

1. пустое значение — пропуск;
2. разрезать по разделителям `,` `;` `#` `доб` `вн.` `ext` (регистр не важен) — **до** извлечения цифр;
3. каждый кусок обрабатывается независимо;
4. удалить всё, кроме `0-9`;
5. меньше 10 цифр — ключа нет;
6. взять последние 10 цифр;
7. дедупликация в пределах записи.

**Ограничения GraalJS, которые определяют, как писать скрипт:**

- ⚠️ **Исправлено по итогам Task 1 — исходное утверждение плана не подтвердилось.** Планировалось, что методы `Array.prototype` на загруженных значениях недоступны. Прогон на настоящем GraalJS 24.2.2 показал обратное: `value.load(...)` действительно отдаёт `java.util.ArrayList` и `LinkedHashMap`, но опция `js.foreign-object-prototype` в этой версии включена по умолчанию, а платформа её не выключает — единственная настройка контекста в `EcosScriptsInitializer` (ecos-webapp-lib) это `js.nashorn-compat`, и она выключена. Поэтому загруженный список для скрипта — полноценный массив: `Array.isArray` даёт `true`, `map`, `filter`, `indexOf`, `join` работают. Поведение зафиксировано тестом `ComputedScriptRunnerTest.loadedMultipleAttributeBehavesAsAnArray`, чтобы смена версии GraalJS не прошла незамеченной.
- ⚠️ **А вот это ограничение настоящее, найдено там же:** у загруженного JSON-объекта (`LinkedHashMap`) чтение по ключу работает, а `Object.keys(obj)` возвращает пустой список — перебрать поля объекта нельзя, обращаться можно только к заранее известным именам. Зафиксировано тестом `loadedJsonObjectIsReadableByKeyButNotEnumerableByObjectKeys`.
- Стиль из решения по Task 2 остаётся в силе: перебирать обычным `for (var i = 0; i < arr.length; i++)`, результат собирать в литерал `[]` через `push`. Это работает при любом значении `js.foreign-object-prototype`, то есть не зависит от версии GraalJS на стенде.
- Обёртка скрипта в `(function(){…})()` ставится, только если в тексте есть подстрока `"return "` — с пробелом. `return[...]` не сработает.
- Массив доезжает до колонки без схлопывания, но держится это на поэлементной обработке в `RecordComputedAttsService.convertResult`: `mapAttributeTypeToRecordComputedResType` для TEXT выдаёт `RecordComputedAttResType.TEXT` и про `multiple` не знает. Поэтому Task 1 и Task 2 проверяют именно возврат массива, а не только отдельные ключи.

**Источники номеров:**

- `opportunity` — только `contacts[].contactPhone`. Депрекированное `deal.phone` **не** источник: оно заполнено у 1213 из 4280 сделок, но в выборке 40 записей во всех случаях дословно совпадает с `contacts[0].contactPhone`, то есть уже зеркалировано;
- `ecos-counterparty` — `phone`, `cellPhone` и `contacts[].contactPhone`, все три сливаются в один массив;
- скрипт на `opportunity` **не** читает `counterparty.phoneDigits` — иначе вернётся проблема устаревания значения.

**Осторожно при замерах:** предикат `empty` на JSON multiple считает пустой массив `[]` непустым, поэтому доли заполненности контактов измеряются только выборкой, а не счётчиком.

**Про сборку при живом стенде:** общее правило запрещает запускать `mvn` в рабочем дереве с поднятым стендом. Здесь риск минимален — оба проекта собирают только zip с артефактами и не поднимают Spring, — но если в том же дереве работает другая сессия, о сборке нужно договориться.

## Post-Completion

**Ручная проверка:**

- ревью скриптов нормализации в обоих YAML на предмет посимвольного совпадения — автоматический тест сверяет поведение, но не тексты;
- проверка на данных прода после выкладки: заполнить `phoneDigits` у 712 лидов, 4280 сделок и 720 контрагентов, оценить время выполнения группового действия под нагрузкой.

**Внешние системы и смежные задачи:**

- COREDEV-510 обязан применять ровно этот же алгоритм к `from.number` из события Mango — это третья копия, и тестами из этого плана она не покрывается;
- COREDEV-510 обязан иметь заранее определённое поведение при нескольких совпадениях: один номер законно принадлежит нескольким записям (две сделки одного клиента, общий номер приёмной) и незаконно (номера-заглушки вроде `+7 999 999 99 99`);
- решить, нужна ли аналогичная правка журнала в `ecos-crm-citeck` (см. Task 8);
- выяснить версию JDK на агенте Jenkins: если она 17 или выше, профиль можно будет убрать и сделать тесты безусловными.
